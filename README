## Available Scripts

In the project directory, you can run:

### `npm start`

### `npm test`

### `npm run build`


## Para ejeutar el sistema con docker , lo hacemos a taves del sisguiente comando
## Los requerimientos son: docker, docker-compose
## Internamente ejecuta 2 servicios, la api y una bd de mongo con la que funciona la api
## El api expone el puerto 3000

docker-compose up
